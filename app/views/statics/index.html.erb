<%= link_to "dashboard", videos_path %>

<p>hello gag9-tv</p>

<table>
  <thead>
    <tr>
      <td> # </td>
      <td> Title</td>
      <td> Descroption </td>
      <td> # </td>
    </tr>
  </thead>
  <tbody>
    <% @videos.each do |v| %>
    <tr>
      <td> # </td>
      <td><%= v.name %></td>
      <td><%= v.url %></td>
      <td><%= v.description %></td>

    </tr>
    <% end %>
  </tbody>
</table>

